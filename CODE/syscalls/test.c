#include <stdio.h>
#include <string.h>
#include <malloc.h>

#include "syscalls.h"

int main(void) {

	//char str[30];

//	errno = -1;

//	char *p = (char *) malloc(100);

/*
 * for (int i = 0; i < 100; i++) {
		p[i] = 'A';
	}
	*/

	volatile float f = 1.0;

	f = f + 2.0f;
}
